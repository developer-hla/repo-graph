# CLI Reference

<!-- Generated by scripts/generate-docs.py. Do not edit by hand. -->
This file is generated from `repo_graph.cli.build_parser`.

## Root Usage

```text
repo-graph [-h]
                  {inspect,sync,build,refresh,serve,load,stats,agent-instructions,report,snapshot,source-graphs} ...
```

## Command Tree

| Command | Help |
| --- | --- |
| `repo-graph inspect` | Validate and summarize a source config. |
| `repo-graph sync` | Clone or update Git sources into the local cache. |
| `repo-graph build` | Build a portable graph JSON file. |
| `repo-graph refresh` | Build from source artifacts and optionally load changed sources into Neo4j. |
| `repo-graph serve` | Run the local Repo Graph HTTP API. |
| `repo-graph load` | Load a graph JSON export into Neo4j. |
| `repo-graph stats` | Read Repo Graph counts from Neo4j. |
| `repo-graph agent-instructions` | Print a Markdown Repo Graph agent snippet. |
| `repo-graph report` | Generate reports from a graph JSON export. |
| `repo-graph report unresolved` | Group unresolved graph edges. |
| `repo-graph report interactions` | Group application and database interaction edges. |
| `repo-graph report database-reconciliation` | Compare code and SQL evidence with current database metadata. |
| `repo-graph report blast-radius` | Trace dependency paths around one graph entity. |
| `repo-graph snapshot` | Inspect or write source snapshots. |
| `repo-graph snapshot status` | Compare current sources to snapshots. |
| `repo-graph snapshot write` | Write current source snapshots. |
| `repo-graph source-graphs` | Build source-level graph artifacts. |
| `repo-graph source-graphs write` | Write one graph artifact per source. |

## `repo-graph inspect`

Validate and summarize a source config.

```text
repo-graph inspect [-h] --config CONFIG
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--config` | yes |  | Path to the Repo Graph source config. |

## `repo-graph sync`

Clone or update Git sources into the local cache.

```text
repo-graph sync [-h] --config CONFIG
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--config` | yes |  | Path to the Repo Graph source config. |

## `repo-graph build`

Build a portable graph JSON file.

```text
repo-graph build [-h] --config CONFIG [--output OUTPUT] [--sync] [--strict] [--cached]
                        [--max-file-bytes MAX_FILE_BYTES]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--config` | yes |  | Path to the Repo Graph source config. |
| `--output` | no |  | Graph JSON output path. |
| `--sync` | no | `false` | Sync Git sources before scanning. |
| `--strict` | no | `false` | Fail if any configured source cannot be scanned. |
| `--cached` | no | `false` | Reuse unchanged source graph artifacts. |
| `--max-file-bytes` | no | `1000000` | Maximum file size to scan. |

## `repo-graph refresh`

Build from source artifacts and optionally load changed sources into Neo4j.

```text
repo-graph refresh [-h] --config CONFIG [--output OUTPUT] [--sync] [--strict] [--load]
                          [--max-file-bytes MAX_FILE_BYTES]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--config` | yes |  | Path to the Repo Graph source config. |
| `--output` | no |  | Graph JSON output path. |
| `--sync` | no | `false` | Sync Git sources before scanning. |
| `--strict` | no | `false` | Fail if any configured source cannot be scanned. |
| `--load` | no | `false` | Load the refreshed graph into Neo4j. |
| `--max-file-bytes` | no | `1000000` | Maximum file size to scan. |

## `repo-graph serve`

Run the local Repo Graph HTTP API.

```text
repo-graph serve [-h] [--config CONFIG] [--host HOST] [--port PORT]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--config` | no |  | Path to the Repo Graph source config. |
| `--host` | no | `127.0.0.1` | Host interface to bind. |
| `--port` | no | `8000` | Port to bind. |

## `repo-graph load`

Load a graph JSON export into Neo4j.

```text
repo-graph load [-h] --graph GRAPH [--append] [--replace-source REPLACE_SOURCE]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--graph` | yes |  | Graph JSON file to load. |
| `--append` | no | `false` | Keep existing Repo Graph data in Neo4j. |
| `--replace-source` | no |  | Replace one source's Neo4j data from a globally resolved graph. Repeat for multiple sources. |

## `repo-graph stats`

Read Repo Graph counts from Neo4j.

```text
repo-graph stats [-h]
```

## `repo-graph agent-instructions`

Print a Markdown Repo Graph agent snippet.

```text
repo-graph agent-instructions [-h] [--api-url API_URL] [--config CONFIG]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--api-url` | no | `http://localhost:8000` | Base URL agents should use. |
| `--config` | no |  | Expected Repo Graph source config path. |

## `repo-graph report`

Generate reports from a graph JSON export.

```text
repo-graph report [-h] {unresolved,interactions,database-reconciliation,blast-radius} ...
```

| Subcommand | Help |
| --- | --- |
| `unresolved` | Group unresolved graph edges. |
| `interactions` | Group application and database interaction edges. |
| `database-reconciliation` | Compare code and SQL evidence with current database metadata. |
| `blast-radius` | Trace dependency paths around one graph entity. |

## `repo-graph report unresolved`

Group unresolved graph edges.

```text
repo-graph report unresolved [-h] --graph GRAPH [--source SOURCE] [--edge-type EDGE_TYPE] [--limit LIMIT]
                                    [--examples EXAMPLES]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--graph` | yes |  | Graph JSON file to report on. |
| `--source` | no |  | Only include unresolved edges from this source. |
| `--edge-type` | no |  | Only include unresolved edges of this type. |
| `--limit` | no | `50` | Maximum unresolved groups to return. |
| `--examples` | no | `3` | Examples to include per unresolved group. |

## `repo-graph report interactions`

Group application and database interaction edges.

```text
repo-graph report interactions [-h] --graph GRAPH [--source SOURCE] [--target-source TARGET_SOURCE]
                                      [--edge-type EDGE_TYPE] [--limit LIMIT] [--examples EXAMPLES]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--graph` | yes |  | Graph JSON file to report on. |
| `--source` | no |  | Only include interactions from this source. |
| `--target-source` | no |  | Only include interactions targeting this source. |
| `--edge-type` | no |  | Only include interactions of this type. |
| `--limit` | no | `50` | Maximum interaction groups to return. |
| `--examples` | no | `3` | Examples to include per group. |

## `repo-graph report database-reconciliation`

Compare code and SQL evidence with current database metadata.

```text
repo-graph report database-reconciliation [-h] --graph GRAPH [--source SOURCE]
                                                 [--database-source DATABASE_SOURCE] [--limit LIMIT]
                                                 [--examples EXAMPLES]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--graph` | yes |  | Graph JSON file to report on. |
| `--source` | no |  | Only include code and schema evidence from this source. |
| `--database-source` | no |  | Only include current database metadata from this source. |
| `--limit` | no | `50` | Maximum reconciliation groups to return. |
| `--examples` | no | `3` | Examples to include per group. |

## `repo-graph report blast-radius`

Trace dependency paths around one graph entity.

```text
repo-graph report blast-radius [-h] --graph GRAPH --entity-id ENTITY_ID [--direction {in,out,both}]
                                      [--edge-type EDGE_TYPE] [--profile {all,impact,structural}] [--depth DEPTH]
                                      [--limit LIMIT]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--graph` | yes |  | Graph JSON file to report on. |
| `--entity-id` | yes |  | Entity ID to use as the blast-radius root. |
| `--direction` | no | `in` | Traversal direction from the root entity. |
| `--edge-type` | no |  | Only traverse this edge type. |
| `--profile` | no | `impact` | Edge profile to traverse when --edge-type is not set. |
| `--depth` | no | `2` | Traversal depth, from 1 to 3. |
| `--limit` | no | `100` | Maximum paths to return. |

## `repo-graph snapshot`

Inspect or write source snapshots.

```text
repo-graph snapshot [-h] {status,write} ...
```

| Subcommand | Help |
| --- | --- |
| `status` | Compare current sources to snapshots. |
| `write` | Write current source snapshots. |

## `repo-graph snapshot status`

Compare current sources to snapshots.

```text
repo-graph snapshot status [-h] --config CONFIG [--sync] [--max-file-bytes MAX_FILE_BYTES]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--config` | yes |  | Path to the Repo Graph source config. |
| `--sync` | no | `false` | Sync Git sources before snapshotting. |
| `--max-file-bytes` | no | `1000000` | Maximum file size to include in snapshot hashing. |

## `repo-graph snapshot write`

Write current source snapshots.

```text
repo-graph snapshot write [-h] --config CONFIG [--sync] [--max-file-bytes MAX_FILE_BYTES]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--config` | yes |  | Path to the Repo Graph source config. |
| `--sync` | no | `false` | Sync Git sources before snapshotting. |
| `--max-file-bytes` | no | `1000000` | Maximum file size to include in snapshot hashing. |

## `repo-graph source-graphs`

Build source-level graph artifacts.

```text
repo-graph source-graphs [-h] {write} ...
```

| Subcommand | Help |
| --- | --- |
| `write` | Write one graph artifact per source. |

## `repo-graph source-graphs write`

Write one graph artifact per source.

```text
repo-graph source-graphs write [-h] --config CONFIG [--sync] [--max-file-bytes MAX_FILE_BYTES]
```

| Option | Required | Default | Help |
| --- | --- | --- | --- |
| `--config` | yes |  | Path to the Repo Graph source config. |
| `--sync` | no | `false` | Sync Git sources before scanning. |
| `--max-file-bytes` | no | `1000000` | Maximum file size to scan. |
